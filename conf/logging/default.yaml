# log frequency
# val_check_interval: 5
### modification
val_check_interval: 1
### modification
progress_bar_refresh_rate: 50

wandb:
  name: ${expname}
  project: Diffhedron
  entity: sadmanomee
  log_model: True
  # mode: 'offline'
  mode: "disabled"
  # mode: "online"
  group: ${expname}

wandb_watch:
  log: "all"
  log_freq: 10

lr_monitor:
  logging_interval: "step"
  log_momentum: False
